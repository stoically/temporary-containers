{
  "name": "tmpcontainers",
  "version": "0.0.0",
  "description": "Temporary Containers",
  "scripts": {
    "coverage": "nyc report --reporter=text-lcov | coveralls",
    "build": "rm -rf web-ext-artifacts .cache dist && cp README.md LICENSE src/core && npm run lint && npm test && parcel build src/ui/options.html --no-minify --no-source-maps && web-ext build -s dist && addons-linter web-ext-artifacts/temporary_containers-*.zip && ncu",
    "build-sign": "rm -rf web-ext-artifacts .cache dist && cp README.md LICENSE src/core && node build-sign.js && npm run lint && npm test && parcel build src/ui/options.html --no-minify --no-source-maps && web-ext sign --channel unlisted -s dist && git checkout -- src/manifest.json && ncu",
    "lint": "eslint src",
    "test": "nyc --reporter=html --reporter=text mocha --reporter=dot ./test/setup.js test/*.test.js --timeout 60000",
    "test-watch": "mocha ./test/setup.js test/*.test.js --watch --tmp-debug --inspect",
    "test-functional": "mocha ./test/functional/*.test.js --timeout 10000",
    "check-dependencies": "ncu",
    "watch": "rm -rf .cache dist && parcel src/ui/options.html --no-hmr"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/stoically/temporary-containers.git"
  },
  "author": "",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/stoically/temporary-containers/issues"
  },
  "homepage": "https://github.com/stoically/temporary-containers#readme",
  "dependencies": {
    "components": "^0.1.0",
    "delay": "^4.1.0",
    "glob-to-regexp": "^0.4.0",
    "jquery": "^3.3.1",
    "jquery-address": "^1.6.0",
    "p-queue": "^3.0.0",
    "psl": "^1.1.31",
    "semantic-ui": "^2.4.2",
    "semver-compare": "^1.0.0",
    "vue": "^2.6.10"
  },
  "devDependencies": {
    "@vue/component-compiler-utils": "^3.0.0",
    "addons-linter": "^1.7.0",
    "babel-eslint": "^10.0.2",
    "chai": "^4.2.0",
    "coveralls": "^3.0.3",
    "eslint": "^5.16.0",
    "mocha": "^6.0.2",
    "npm-check-updates": "^3.1.3",
    "nyc": "^14.0.0",
    "parcel-bundler": "^1.12.3",
    "parcel-plugin-static-files-copy": "^2.1.2",
    "sinon": "^7.3.1",
    "sinon-chai": "^3.3.0",
    "vue-template-compiler": "^2.6.10",
    "web-ext": "^3.0.0",
    "webextensions-geckodriver": "^0.5.1",
    "webextensions-jsdom": "^0.15.3"
  },
  "browserslist": [
    "Firefox >= 60"
  ],
  "staticFiles": {
    "staticPath": [
      "src/core",
      "src/ui/static"
    ]
  },
  "private": true,
  "nyc": {
    "exclude": [
      "test/**/*.js"
    ]
  }
}
